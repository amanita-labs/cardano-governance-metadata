# Coverage is opt-in via `bun run test:coverage` (used by CI); plain
# `bun test` / `test:watch` skip it for faster local iteration.
[test]
coverageReporter = ["text", "lcov"]

[install]
peer = false
